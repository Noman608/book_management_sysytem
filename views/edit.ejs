<!DOCTYPE html>
<html lang="en">
<head>cls
   <meta charset="UTF-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <link rel="stylesheet" href="/css/styles.css">
   <title>Edit Book</title>
</head>
<body style="text-align: center;">
   <h1>Edit Book</h1>
   <form action="/update/<%= book._id %>" method="POST">
      <div>
         <label for="title">Title:</label>
         <input type="text" id="title" name="title" value="<%= book.title %>" required>
      </div>
      <div>
         <label for="publisher">Publisher:</label>
         <input type="text" id="publisher" name="publisher" value="<%= book.publisher %>" required>
      </div>
      <div>
         <label for="publisherAge">Publisher Age:</label>
         <input type="number" id="publisherAge" name="publisherAge" value="<%= book.publisherAge %>" required>
      </div>
      <div>
         <label for="pageNum">Page Number:</label>
         <input type="number" id="pageNum" name="pageNum" value="<%= book.pageNum %>" required>
      </div>
      <div>
         <label for="publishDate">Publish Date:</label>
         <input type="date" id="publishDate" name="publishDate" value="<%= book.publishDate.toISOString().substring(0, 10) %>" required>
      </div>
      <div>
         <label for="genre">Genre:</label>
         <select id="genre" name="genre" required>
            <option value="sci-fi" <%= book.genre === 'sci-fi' ? 'selected' : '' %>>Sci-Fi</option>
            <option value="drama" <%= book.genre === 'drama' ? 'selected' : '' %>>Drama</option>
            <option value="novel" <%= book.genre === 'novel' ? 'selected' : '' %>>Novel</option>
         </select>
      </div>
      <button type="submit">Update Book</button>
   </form>
   <a href="/booklist">Back to Booklist</a>
</body>
</html>
